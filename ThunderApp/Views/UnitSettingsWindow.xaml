<Window x:Class="ThunderApp.Views.UnitSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="http://schemas.microsoft.com/winfx/2006/xaml/presentation/shell"
        Title="Units and Map Settings"
        Width="560"
        Height="460"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBg}"
        WindowStyle="None"
        ResizeMode="CanResize">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="38" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="Background" Value="{DynamicResource Surface2}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="14,8" />
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Height="38" Background="{DynamicResource AppBg}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="132" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Units and Map Settings" VerticalAlignment="Center" Margin="12,0,0,0" FontWeight="SemiBold" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="—" Width="44" Click="Minimize_OnClick" />
                    <Button Content="▢" Width="44" Click="Maximize_OnClick" />
                    <Button Content="✕" Width="44" Click="Close_OnClick" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Margin="20">
            <TextBlock Text="Set your preferred units and map theme. Changes apply app-wide." FontSize="14" Opacity="0.85" Margin="0,0,0,18" />

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="210" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" FontSize="16" Text="Temperature" />
                <ComboBox Grid.Row="0" Grid.Column="1" x:Name="TemperatureUnitCombo" Margin="0,0,0,14" />

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontSize="16" Text="Wind Speed" />
                <ComboBox Grid.Row="1" Grid.Column="1" x:Name="WindSpeedUnitCombo" Margin="0,0,0,14" />

                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" FontSize="16" Text="Pressure" />
                <ComboBox Grid.Row="2" Grid.Column="1" x:Name="PressureUnitCombo" Margin="0,0,0,14" />

                <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" FontSize="16" Text="Map Theme" />
                <ComboBox Grid.Row="3" Grid.Column="1" x:Name="MapThemeCombo" />
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
                <Button Content="Cancel" Width="110" Margin="0,0,8,0" Click="Cancel_OnClick" />
                <Button Content="Save" Width="110" Click="Save_OnClick" />
            </StackPanel>
        </StackPanel>
    </DockPanel>
</Window>
