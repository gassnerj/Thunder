<Window x:Class="ThunderApp.Views.MapStylingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Map Styling" Height="720" Width="860"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Surface}">
    <Window.Resources>
        <Style x:Key="DashToggleButton" TargetType="ToggleButton">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="160" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="Background" Value="{DynamicResource Surface2}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Accent}" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Accent}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="RowBackground" Value="{DynamicResource Surface2}" />
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource Surface}" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Border}" />
            <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource Surface2}" />
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="10,6" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="Background" Value="{DynamicResource Surface2}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="Background" Value="{DynamicResource Surface2}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource Text}" />
            <Setter Property="Background" Value="{DynamicResource Surface2}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
        </Style>

    </Window.Resources>

    <DockPanel LastChildFill="True" Margin="18">
        <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,14">
            <TextBlock Text="Palette" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource Text}" />
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                <TextBlock Text="Mode:" VerticalAlignment="Center" Foreground="{DynamicResource Text2}" Margin="0,0,10,0"/>
                <ComboBox Width="160" SelectedValue="{Binding PaletteMode, Mode=TwoWay}" SelectedValuePath="Content">
                    <ComboBoxItem Content="Official"/>
                    <ComboBoxItem Content="Custom"/>
                </ComboBox>

                <TextBlock Text="Severity overlay:" VerticalAlignment="Center" Foreground="{DynamicResource Text2}" Margin="24,0,10,0"/>
                <ToggleButton Style="{StaticResource DashToggleButton}" Content="Outline" IsChecked="{Binding ShowSeverityOutline, Mode=TwoWay}" Margin="0,0,10,0"/>
                <ToggleButton Style="{StaticResource DashToggleButton}" Content="Glow" IsChecked="{Binding ShowSeverityGlow, Mode=TwoWay}" Margin="0,0,10,0"/>
                <ToggleButton Style="{StaticResource DashToggleButton}" Content="Stripes" IsChecked="{Binding ShowSeverityStripes, Mode=TwoWay}"/>
            </StackPanel>

            <TextBlock Text="Custom colors: set a hex value like #FF0000. Leave blank to use the official color."
                       Foreground="{DynamicResource Text2}" Margin="0,10,0,0"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="{DynamicResource Surface2}" BorderBrush="{DynamicResource Border}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Event" Binding="{Binding EventName}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Official" Binding="{Binding OfficialHex}" IsReadOnly="True" Width="160"/>
                <DataGridTextColumn Header="Custom" Binding="{Binding CustomHex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Clear Custom" Padding="14,8" Margin="0,0,10,0" Click="ClearCustom_OnClick"/>
            <Button Content="Save" Padding="14,8" Margin="0,0,10,0" Click="Save_OnClick"/>
            <Button Content="Close" Padding="14,8" Click="Close_OnClick"/>
        </StackPanel>
    </DockPanel>
</Window>
